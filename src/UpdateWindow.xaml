<Window x:Class="DeskOrganizer.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="检查更新" Height="420" Width="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E2E">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题和版本信息 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock x:Name="TitleText" Text="正在检查更新..." FontSize="18" FontWeight="Bold" Foreground="#E0E0E0"/>
            <TextBlock x:Name="VersionText" Text="" FontSize="13" Foreground="#888" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- 进度条 -->
        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <ProgressBar x:Name="ProgressBar" Height="4" IsIndeterminate="True" Visibility="Collapsed"/>
            <TextBlock x:Name="ProgressText" Text="" FontSize="12" Foreground="#888" Margin="0,8,0,0" TextAlignment="Center"/>
        </StackPanel>

        <!-- 更新日志 -->
        <Border Grid.Row="2" Background="#252535" CornerRadius="6" Padding="16" Margin="0,0,0,16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ReleaseNotesText" Text="" FontSize="13" Foreground="#CCC" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>

        <!-- 按钮区 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="DownloadButton" Content="立即更新" Width="100" Height="34" Margin="0,0,8,0"
                    Background="#4CAF50" Foreground="White" FontWeight="Bold"
                    BorderThickness="0" Click="DownloadButton_Click" Visibility="Collapsed"/>
            <Button x:Name="OpenBrowserButton" Content="前往下载" Width="100" Height="34" Margin="0,0,8,0"
                    Background="#2196F3" Foreground="White"
                    BorderThickness="0" Click="OpenBrowserButton_Click" Visibility="Collapsed"/>
            <Button x:Name="CloseButton" Content="关闭" Width="80" Height="34"
                    Background="#333" Foreground="#CCC"
                    BorderThickness="0" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
